
export enum UIViewContentMode {
    scaleToFill,
    scaleAspectFit,
    scaleAspectFill,
}

export enum UIControlState {
    normal,
    highlighted,
    disabled,
    selected,
}

export enum UIControlContentVerticalAlignment {
    center,
    top,
    bottom,
    fill,
}

export enum UIControlContentHorizontalAlignment {
    center,
    left,
    right,
    fill,
}

export enum UITextAlignment {
    left,
    center,
    right,
}

export enum UILineBreakMode {
    wordWrapping,
    charWrapping,
    clipping,
    truncatingHead,
    truncatingTail,
    truncatingMiddle,
}

export enum UITextFieldViewMode {
    never,
    whileEditing,
    unlessEditing,
    always,
}

export enum UITextAutocapitalizationType {
    none,
    words,
    sentences,
    allCharacters,
}

export enum UITextAutocorrectionType {
    default,
    no,
    yes,
}

export enum UITextSpellCheckingType {
    default,
    no,
    yes
}

export enum UIKeyboardType {
    default,
    ASCIICapable,
    numbersAndPunctuation,
    numberPad,
    phonePad,
    emailAddress,
    decimalPad
}

export enum UIReturnKeyType {
    default,
    go,
    next,
    send,
    done
}

export enum UILayoutConstraintAxis {
    horizontal,
    vertical,
}

export enum UIStackViewDistribution {
    fill,
    fillEqually,
    fillProportionally,
    equalSpacing,
    equalCentering,
}

export enum UIStackViewAlignment {
    fill,
    leading,
    center,
    trailing,
}

export enum UIStatusBarStyle {
    default,
    lightContent,
}