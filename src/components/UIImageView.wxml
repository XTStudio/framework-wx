<view style='{{style}}' animation='{{animation}}'>
    <image style='position:absolute; width: 100%; height: 100%' src="{{imageSource}}" mode="{{scaleMode}}"></image>
    <view wx:for="{{subviews}}" wx:key="index" wx:for-item="item">
        <factory view="{{item}}"></factory>
    </view>
</view>